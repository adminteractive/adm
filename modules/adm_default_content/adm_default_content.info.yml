name: "ADM Default Content"
description: "Sets up the default content on site install and can be uninstalled later."
core_version_requirement: ^8 || ^9
type: module

dependencies:
  - adm_carousel
  - adm_grid
  - adm_media
  - adm_page
